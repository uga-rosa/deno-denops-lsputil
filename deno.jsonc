{
  "name": "@uga-rosa/denops-lsputil",
  "version": "0.9.4",
  "exports": "./lsputil/mod.ts",
  "lock": false,
  "tasks": {
    "pre-commit": "deno task fmt && deno task check && deno task test",
    "test": "deno test -A",
    "check": "deno check **/*.ts && deno fmt --check **/*.ts && deno lint",
    "fmt": "deno fmt **/*.ts"
  }
}
